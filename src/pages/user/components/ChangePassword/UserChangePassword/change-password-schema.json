{
	"type": "object",
	"properties": {
		"old_password": {
			"type": "string",
			"title": "Ingrese tu contraseña actual",
			"format": "password",
			"minLength": 6,
			"maxLength": 127,
			"errorMessage": {
				"minLength": "Este campo debe tener como mínimo 6 caracteres."
			}
		},
		"new_password": {
			"type": "string",
			"title": "Ingrese su nueva contraseña",
			"format": "password",
			"minLength": 6,
			"maxLength": 127,
			"errorMessage": {
				"minLength": "Este campo debe tener como mínimo 6 caracteres."
			}
		},
		"repeat_new_password": {
			"type": "string",
			"title": "Repita su nueva contraseña",
			"format": "password",
			"minLength": 6,
			"maxLength": 127,
			"errorMessage": {
				"minLength": "Este campo debe tener como mínimo 6 caracteres."
			}
		}
	},
	"required": ["old_password", "new_password", "repeat_new_password"]
}
